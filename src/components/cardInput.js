import React from 'react';

const CardInput = ({onChange,value,onSubmit,obCancel}) => {
  return (
    <div>
      <textarea></textarea>
      <button>Save</button>
      <button>Cancel</button>
    </div>
  )
}

export default CardInput;
